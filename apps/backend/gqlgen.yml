schema:
  - web/graph/*.graphqls

exec:
  filename: web/graph/generated.go
  package: graph

model:
  filename: web/graph/model/models_gen.go
  package: model

resolver:
  layout: follow-schema
  dir: web/graph
  package: graph
  filename_template: "{name}.resolvers.go"
